[pytest]
testpaths = tests
addopts = -v --strict-markers --disable-warnings --alluredir=reports
markers =
    smoke: quick smoke tests
    regression: full regression suite
    negative: negative test cases